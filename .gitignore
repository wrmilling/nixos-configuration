# Local clone
nixpkgs

# In case you nixos-rebuild bulid
result

# AI Stuff
.sisyphus

# CodeGraph local index (db/cache/logs/hook markers are all machine-local,
# regenerated per project via `codegraph init` -- see .codegraph/.gitignore)
.codegraph/
